spring:
  config:
    activate:
      on-profile: test

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  docker:
    compose:
      lifecycle-management: start_only

gg:
  jte:
    developmentMode: true

visitor:
  system:
    otp:
      subject: Test OTP Code
